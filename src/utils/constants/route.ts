export enum BASE_URL {
  CUSTOMER = `/api/v1/customer`,
  VENDOR = `/api/v1/vendor`,
  ADMIN = `/api/v1/admin`,
  AUTH = `/api/v1/auth`,
  SERVICE_CATEGORY = `/api/v1/service_category`,
}

export enum URL_PART {
  customer = 'customer',
  vendor = 'vendor',
  admin = 'admin',
  auth = 'auth',
}
